import React from "react";
import { Typography, Divider } from "antd";

const { Title, Paragraph, Text, Link } = Typography;

const About = () => {
  return <Title level={2}>Introduction</Title>;
};

export default About;
